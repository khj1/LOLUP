<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lolup.lolup_project.member.MybatisMemberRepository">

    <select id="findByEmail" parameterType="java.lang.String" resultType="Member">
        SELECT * FROM member WHERE email = #{_email}
    </select>

    <select id="save" parameterType="Member" resultType="java.lang.String">
        INSERT INTO member (name, email, picture, role)
            values (#{name}, #{email}, #{picture}, #{role})
            RETURNING email;
    </select>

    <update id="update">
        UPDATE member SET summonerName = #{summonerName} WHERE memberId = #{memberId}
    </update>

</mapper>
