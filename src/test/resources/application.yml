spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: false

  main:
    allow-bean-definition-overriding: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test
            client-secret: test
            authorization-grant-type: test
            accessTokenValiditySeconds: test

logging:
  level:
    org.hibernate.SQL: debug

jwt:
  secret: ENC(b/GysgadqZoCeK/Y4ctB8dumwZSuOuf5ppvC3K3v20A=)

jasypt:
  encryptor:
    bean: jasyptStringEncryptor

front:
  domain: test
  redirect_url: test
